# Maigewan CMS 快速开始指南

## 🚀 快速安装

### 方法 1: 使用 Git 克隆

```bash
# 克隆仓库
git clone https://github.com/3mmmbulus/mg-bludit.git

# 进入目录
cd mg-bludit

# 设置权限 (Linux/Mac)
chmod -R 755 bl-content
```

### 方法 2: 下载 ZIP

1. 访问: https://github.com/3mmmbulus/mg-bludit
2. 点击 "Code" → "Download ZIP"
3. 解压到您的 Web 服务器目录
4. 设置 `bl-content` 目录可写权限

## 📝 安装步骤

1. **访问安装向导**
   ```
   http://your-domain.com/install.php
   ```

2. **选择语言**
   - 支持 30+ 种语言
   - 推荐中文用户选择 "简体中文 (zh_CN)"

3. **设置管理员账户**
   - 用户名: admin (可自定义)
   - 密码: 设置强密码
   - 邮箱: 您的邮箱地址

4. **完成安装**
   - 点击"安装"按钮
   - 等待安装完成

## 🎯 首次登录

访问后台管理:
```
http://your-domain.com/admin/
```

使用安装时设置的用户名和密码登录。

## 🛠️ 服务器配置

### Apache (.htaccess)

Maigewan CMS 包含默认的 `.htaccess` 文件，无需额外配置。

### Nginx

在 `server` 块中添加以下配置:

```nginx
location / {
    try_files $uri $uri/ /index.php?$args;
}

location ~ \.php$ {
    fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
    fastcgi_index index.php;
    include fastcgi_params;
}

# 保护敏感目录
location ~ /bl-content/(databases|workspaces|pages|tmp)/.*\.(php|txt|md)$ {
    deny all;
}
```

## 📊 系统要求检查

运行测试脚本验证环境:

```bash
php php83_compatibility_test.php
```

应该看到所有测试都通过 ✓

## 🎨 自定义主题

1. 登录后台
2. 前往 "主题" 部分
3. 选择并激活您喜欢的主题
4. 或上传自己的主题到 `bl-themes/` 目录

## 🔌 启用插件

1. 登录后台
2. 前往 "插件" 部分
3. 点击想要启用的插件
4. 配置插件设置
5. 保存更改

## 📄 创建第一篇文章

1. 登录后台
2. 点击 "新建内容"
3. 填写标题和内容
4. 选择分类和标签（可选）
5. 点击 "发布"

## 🔧 常见问题

### 权限错误

如果遇到权限问题:

```bash
# Linux/Mac
chmod -R 755 bl-content
chown -R www-data:www-data bl-content

# 或者
chmod -R 777 bl-content  # 仅用于测试，不推荐生产环境
```

### PHP 版本问题

确保使用 PHP 8.0 或更高版本:

```bash
php -v
```

### 500 错误

1. 检查 PHP 错误日志
2. 确保所有必需的 PHP 扩展已安装
3. 验证文件权限正确
4. 运行兼容性测试脚本

## 📚 更多资源

- **官方文档**: https://docs.bludit.com
- **GitHub 仓库**: https://github.com/3mmmbulus/mg-bludit
- **问题反馈**: https://github.com/3mmmbulus/mg-bludit/issues

## 🎓 下一步

- 探索管理面板
- 配置网站设置
- 创建页面和文章
- 自定义主题外观
- 启用需要的插件

---

**欢迎使用 Maigewan CMS！** 🎉

如有问题，请访问 GitHub 提交 Issue。
