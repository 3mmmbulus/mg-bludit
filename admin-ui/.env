# 应用的基础路径，默认为 "/"
# 如果使用子目录，必须以 "/" 结尾，例如 "/admin/" 而不是 "/admin"
VITE_BASE_URL=/

VITE_APP_TITLE=Maigewan Admin

VITE_APP_DESC=Maigewan CMS - Maigewan站群管理系统

# 图标名称的前缀
VITE_ICON_PREFIX=icon

# 本地 svg 图标组件的前缀，必须包含 VITE_ICON_PREFIX
# 格式：{VITE_ICON_PREFIX}-{本地图标名称}
VITE_ICON_LOCAL_PREFIX=icon-local

# 权限路由模式：static（静态）｜ dynamic（动态）
VITE_AUTH_ROUTE_MODE=static

# 静态权限路由的首页
VITE_ROUTE_HOME=home

# 默认菜单图标
VITE_MENU_ICON=mdi:menu

# 开发模式下是否启用 HTTP 代理
VITE_HTTP_PROXY=Y

# vue-router 模式：hash | history | memory
VITE_ROUTER_HISTORY_MODE=history

# 后端服务成功响应码，收到此码时表示请求成功
VITE_SERVICE_SUCCESS_CODE=0000

# 后端服务登出码，收到此码时用户将被登出并重定向到登录页
VITE_SERVICE_LOGOUT_CODES=8888,8889

# 后端服务模态框登出码，收到此码时将通过模态框提示用户已登出
VITE_SERVICE_MODAL_LOGOUT_CODES=7777,7778

# 后端服务 token 过期码，收到此码时将刷新 token 并重新发送请求
VITE_SERVICE_EXPIRED_TOKEN_CODES=9999,9998,3333

# 静态路由模式下定义的超级管理员角色
VITE_STATIC_SUPER_ROLE=R_SUPER

# 是否生成 sourcemap
VITE_SOURCE_MAP=N

# 用于区分不同域名下的存储
VITE_STORAGE_PREFIX=MGW_

# 是否自动检测程序更新
VITE_AUTOMATICALLY_DETECT_UPDATE=Y

# 是否在终端显示代理 URL 日志
VITE_PROXY_LOG=Y

# 控制是否启动编辑器
# 注意：此插件仅在开发模式下可用，构建模式下不可用
VITE_DEVTOOLS_LAUNCH_EDITOR=code
